Shorten the Reconciliation History menu item labels to single words:
- "Review cleared transactions" → "Review"
- "Undo reconciliation" → "Undo"

File: `src/components/transactions/ReconcileAccountsContent.tsx` — update the two `label` strings in the `TableRowActions` actions array. No other changes.