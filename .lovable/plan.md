Re-emit the existing `NobHillCourt_Costs_By_CostCode.xlsx` artifact so it opens in the right-side Files preview panel. No code changes — just present the artifact tag pointing to `/mnt/documents/NobHillCourt_Costs_By_CostCode.xlsx` with the spreadsheet MIME type. Clicking it opens the platform's xlsx viewer which renders all rows.