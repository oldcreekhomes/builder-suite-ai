## Fix

Revert the share URL generated by the Folder Share modal back to the Supabase `share-redirect` edge function format, matching what it was before today's testing change.

### Change
In `src/components/files/components/FolderShareModal.tsx` (and any other place I set it to `window.location.origin` for folder shares today), set the generated link back to:

```
https://nlmnwlvmmkngrgatnzkj.supabase.co/functions/v1/share-redirect?id=<shareId>&type=f
```

No other behavior changes — folder hierarchy fix stays intact; only the copied/displayed URL reverts to the Supabase redirect form with no `lovable.app` reference.